<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
    <meta charset="UTF-8">
    <title>لوحة التحكم</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Tajawal&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Tajawal', sans-serif;
            background-color: #f8f9fa;
        }

        .post-content {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 15px;
            margin-bottom: 20px;
            direction: rtl;
            text-align: right;
        }

        /* تخصيص blockquote */
        .post-content blockquote {
            border-right: 5px solid #ccc;
            padding-right: 15px;
            margin-right: 0;
            margin-left: 0;
            direction: rtl;
            text-align: right;
        }

        /* إزالة الحد الأيسر الافتراضي */
        .post-content blockquote[dir="rtl"] {
            border-left: none;
            border-right: 5px solid #ccc;
        }
    </style>
</head>

<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
        <div class="container">
            <a class="navbar-brand" href="#">لوحة التحكم</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMenu">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navMenu">
                <ul class="navbar-nav ms-auto mb-1 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/posts/create">➕ إنشاء تدوينة</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/posts/select-edit">✏️ تعديل تدوينة</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/posts/select-delete">🗑️ حذف تدوينة</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-danger" href="/logout">تسجيل الخروج</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- محتوى الصفحة -->
    <div class="container">
        <h2 class="mb-4">مرحبًا <%= user.username %> 👋</h2>

        <h4 class="mb-3">📌 تدويناتك</h4>

        <% if (posts.length === 0) { %>
            <div class="alert alert-info">لا توجد تدوينات بعد.</div>
        <% } else { %>
            <% posts.forEach(post => { %>
                <div class="post-content">
                    <h5><%= post.title %></h5>
                    <div><%- post.content %></div>
                    <small class="text-muted">بتاريخ: <%= new Date(post.created_at).toLocaleString() %></small>
                </div>
            <% }); %>
        <% } %>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>