<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title>مدونتي</title>
  <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/js/css/home.css">
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar">
    <div class="navbar-container">
      <ul class="nav-links">
        <li><a href="/">الرئيسية</a></li>
        <li><a href="/about">النبذة</a></li>
        <li><a href="/contact">التواصل</a></li>
      </ul>
      <a href="/" class="logo">مدونتي</a>
    </div>
  </nav>

  <!-- محتوى الصفحة -->
  <div class="container">
    <% if (posts.length === 0) { %>
      <p>لا توجد تدوينات بعد.</p>
    <% } else { %>
      <% posts.forEach(post => { %>
        <div class="post-box">
          <h2><%= post.title %></h2>
          <p><%= post.content.substring(0, 150).replace(/(<([^>]+)>)/gi, "") %>...</p>
          <div class="post-meta">نُشرت في <%= post.created_at.toLocaleDateString() %></div>
          <a href="/posts/<%= post.id %>" class="read-more">قراءة المزيد</a>
        </div>
      <% }) %>
    <% } %>
  </div>

</body>
</html>
