<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title><%= site.title %> - <%= post.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1><%= post.title %></h1>
        <p class="text-muted">بقلم: <%= post.username %> | <%= new Date(post.created_at).toLocaleDateString() %></p>
        <hr>
        <p><%= post.content %></p>
        <a href="/" class="btn btn-secondary mt-3">العودة للرئيسية</a>
    </div>
</body>
</html>
