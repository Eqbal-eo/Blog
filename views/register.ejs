<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>إكمال التسجيل - مدونات آفاق</title>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        /* login.css */
        html,
        body {
            height: 100%;
            margin: 0;
            padding: 0;
            font-family: 'Tajawal', sans-serif;
            display: flex;
            flex-direction: column;
            background-color: #f9f7f0;
        }

        /* Card styling */
        .card {
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        /* btn-dark button */
        .btn-dark {
            background-color: #8c4a21ca;
            border-color: transparent;
        }

        .btn-dark:hover {
            background-color: #470539c5;
            border-color: transparent;
        }

        /* Headings styling */
        h1 {
            font-family: 'Tajawal', sans-serif;
            font-size: 2.5rem;
            color: #8c4a21ca;
            text-align: center;
            margin-top: 50px;
            padding: 0 15px;
        }

        h2 {
            font-family: 'Tajawal', sans-serif;
            font-size: 1.75rem;
            color: #5a5a5a;
            text-align: center;
            margin-top: 1px;
            padding: 0 15px;
        }

        .container {
            margin-top: 50px;
            margin-bottom: 50px;
            padding: 0 15px;
        }

        /* Input fields styling */
        .form-control {
            font-size: 0.9rem;
            text-align: right;
            direction: rtl;
        }

        .form-label {
            margin-bottom: 0.3rem;
        }

        .form-control::placeholder {
            font-size: 0.85rem;
            text-align: right;
            direction: rtl;
            opacity: 0.7;
        }

        .mb-3 {
            margin-bottom: 1rem;
        }

        #invite_code {
            text-transform: uppercase;
            letter-spacing: 2px;
            font-weight: bold;
            text-align: center;
        }

        /* Responsive design for mobile and tablet */
        @media (max-width: 768px) {
            h1 {
                font-size: 1.8rem;
                margin-top: 20px;
            }

            h2 {
                font-size: 1.3rem;
                margin-top: 10px;
            }

            .container {
                margin-top: 20px;
                margin-bottom: 40px;
                padding: 0 15px;
            }
        }

        @media (max-width: 480px) {
            h1 {
                font-size: 1.5rem;
                margin-top: 15px;
            }

            h2 {
                font-size: 1.1rem;
            }
        }

        /* Footer styles */
        footer {
            flex-shrink: 0;
            background-color: #ffffff;
            text-align: center;
            padding: 1rem 0;
            font-size: 1rem;
            color: #5e5d5d;
            width: 100%;
            box-shadow: 0 -2px 6px rgba(0, 0, 0, 0.1);
            border-top: 1px solid #cccccc96;
            margin-top: auto;
        }

        footer a {
            color: #8c4a21ca;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        footer a:hover {
            color: #470539c5;
        }

        /* Style links */
        a {
            text-decoration: none !important;
            transition: color 0.3s ease;
        }

        a:hover {
            color: #007bff !important;
        }
    </style>
</head>

<body class="bg-light">
    <h1><a href="/" style="text-decoration: none; color: inherit;">مدونات آفاق</a></h1>
    <h2>إكمال التسجيل</h2>

    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-10 col-sm-8 col-md-4">

                <div class="card shadow">
                    <div class="card-header text-center text-dark">
                        <h6 class="text-muted">أدخل بياناتك لإكمال إنشاء الحساب</h6>
                    </div>

                    <div class="card-body">

                        <% if (error) { %>
                            <div class="alert alert-danger text-center">
                                <%= error %>
                            </div>
                            <% } %>

                                <% if (typeof success !=='undefined' && success) { %>
                                    <div class="alert alert-success text-center">
                                        <%= success %>
                                    </div>
                                    <% } %>

                                        <form action="/register" method="POST">
                                            <div class="mb-3">
                                                <label for="username" class="form-label">
                                                    <i class="fas me-2"></i>اسم المستخدم
                                                </label>
                                                <input type="text" class="form-control" id="username" name="username"
                                                    placeholder="أدخل اسم المستخدم" required>
                                                <small class="text-muted">سيظهر هذا الاسم في URL مدونتك</small>
                                            </div>

                                            <div class="mb-3">
                                                <label for="email" class="form-label">
                                                    <i class="fas me-2"></i>البريد الإلكتروني
                                                </label>
                                                <input type="email" class="form-control" id="email" name="email"
                                                    placeholder="أدخل البريد الإلكتروني" required>
                                            </div>

                                            <div class="mb-3">
                                                <label for="invite_code" class="form-label">
                                                    <i class="fas me-2"></i>كود التفعيل
                                                </label>
                                                <input type="text" class="form-control" id="invite_code"
                                                    name="invite_code" placeholder="*****" required maxlength="6">
                                                <small class="text-muted">أدخل الكود المكون من 6 أحرف المرسل إليك عبر
                                                    البريد الإلكتروني</small>
                                            </div>

                                            <div class="d-grid">
                                                <button type="submit" class="btn btn-dark">
                                                    <i class="fas me-2"></i>إكمال التسجيل
                                                </button>
                                            </div>
                                        </form>
                                        <div class="mt-4 text-center">
                                            <small class="text-muted">
                                                ليس لديك حساب؟
                                                <a href="/blog-request" class="text-dark">اطلب إنشاء مدونة</a>
                                            </small>
                                        </div>

                                        <div class="mt-1 text-center">
                                            <small class="text-muted">
                                                لديك حساب؟
                                                <a href="/login" class="text-dark">سجّل الدخول</a>
                                            </small>
                                        </div>

                    </div>
                </div>

            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer>
        مدونات آفاق | منصة للتدوين الثقافي والفكري © 2025
    </footer>

    <script>
        // Format activation code field
        document.getElementById('invite_code').addEventListener('input', function() {
            this.value = this.value.toUpperCase().replace(/[^A-Z0-9]/g, '');
        });
    </script>
</body>

</html>