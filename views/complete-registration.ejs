<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>إكمال التسجيل - مدونات آفاق</title>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        html, body {
            height: 100%;
            margin: 0;
            padding: 0;
            font-family: 'Tajawal', sans-serif;
            display: flex;
            flex-direction: column;
            background-color: #f9f7f0;
        }
        .card {
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .btn-dark {
            background-color: #8c4a21ca;
            border-color: transparent;
        }
        .btn-dark:hover {
            background-color: #470539c5;
            border-color: transparent;
        }
        h1 {
            font-family: 'Tajawal', sans-serif;
            font-size: 2.5rem;
            color: #8c4a21ca;
            text-align: center;
            margin-top: 30px;
            padding: 0 15px;
        }
        .container {
            margin-top: 30px;
            margin-bottom: 50px;
            padding: 0 15px;
        }
        .form-control {
            font-size: 0.9rem;
            text-align: right;
            direction: rtl;
        }
        .form-label {
            font-weight: 600;
            color: #333;
        }
        footer {
            flex-shrink: 0;
            background-color: #ffffff;
            text-align: center;
            padding: 1rem 0;
            font-size: 1rem;
            color: #5e5d5d;
            width: 100%;
            box-shadow: 0 -2px 6px rgba(0, 0, 0, 0.1);
            border-top: 1px solid #cccccc96;
            margin-top: auto;
        }
        footer a {
            color: #8c4a21ca;
            text-decoration: none;
            transition: color 0.3s ease;
        }
        footer a:hover {
            color: #470539c5;
        }
        .progress-steps {
            display: flex;
            justify-content: center;
            margin-bottom: 2rem;
        }
        .step {
            text-align: center;
            position: relative;
            flex: 1;
            max-width: 150px;
        }
        .step-circle {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background-color: #28a745;
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 0.5rem;
            font-weight: bold;
        }
        .step-current .step-circle {
            background-color: #8c4a21ca;
        }
        .step-text {
            font-size: 0.85rem;
            color: #666;
        }
        .code-input {
            text-align: center;
            font-size: 1.5rem;
            letter-spacing: 0.5rem;
            font-weight: bold;
        }
        .user-info {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 1rem;
            margin-bottom: 1rem;
        }
    </style>
</head>

<body class="bg-light">
    <h1><a href="/" style="text-decoration: none; color: inherit;">مدونات آفاق</a></h1>

    <div class="container mt-4">
        <!-- Progress Steps -->
        <div class="progress-steps">
            <div class="step">
                <div class="step-circle"><i class="fas fa-check"></i></div>
                <div class="step-text">طلب المدونة</div>
            </div>
            <div class="step">
                <div class="step-circle"><i class="fas fa-check"></i></div>
                <div class="step-text">المراجعة</div>
            </div>
            <div class="step">
                <div class="step-circle"><i class="fas fa-check"></i></div>
                <div class="step-text">كود التفعيل</div>
            </div>
            <div class="step step-current">
                <div class="step-circle">4</div>
                <div class="step-text">إنشاء الحساب</div>
            </div>
        </div>

        <div class="row justify-content-center">
            <div class="col-12 col-md-6 col-lg-5">
                <div class="card shadow">
                    <div class="card-header text-center bg-dark text-white">
                        <h4><i class="fas fa-key me-2"></i>إكمال التسجيل</h4>
                        <small>أدخل كود التفعيل المرسل إليك</small>
                    </div>

                    <div class="card-body">
                        <% if (error) { %>
                            <div class="alert alert-danger text-center">
                                <%= error %>
                            </div>
                        <% } %>

                        <% if (success) { %>
                            <div class="alert alert-success text-center">
                                <%= success %>
                            </div>
                        <% } %>

                        <!-- معلومات المستخدم إذا كان الكود صحيحاً -->
                        <% if (typeof userInfo !== 'undefined' && userInfo) { %>
                            <div class="user-info">
                                <h6 class="text-dark mb-2"><i class="fas fa-user me-2"></i>مرحباً <%= userInfo.full_name %>!</h6>
                                <p class="mb-1 small"><strong>البريد الإلكتروني:</strong> <%= userInfo.email %></p>
                                <p class="mb-0 small"><strong>التخصص:</strong> <%= userInfo.specialty %></p>
                            </div>
                        <% } %>

                        <form method="POST" action="/complete-registration">
                            <div class="mb-4">
                                <label for="invite_code" class="form-label">
                                    <i class="fas fa-key me-2"></i>كود التفعيل
                                </label>
                                <input type="text" class="form-control code-input" id="invite_code" name="invite_code" 
                                       placeholder="XXXXXX" maxlength="6" required 
                                       value="<%= typeof invite_code !== 'undefined' ? invite_code : '' %>">
                                <small class="text-muted">أدخل الكود المكون من 6 أرقام المرسل إليك عبر البريد الإلكتروني</small>
                            </div>

                            <% if (typeof userInfo !== 'undefined' && userInfo) { %>
                                <!-- إذا كان الكود صحيحاً، اعرض نموذج إنشاء الحساب -->
                                <div class="mb-3">
                                    <label for="username" class="form-label">اسم المستخدم</label>
                                    <input type="text" class="form-control" id="username" name="username" 
                                           placeholder="اختر اسم المستخدم الذي تريد" required>
                                    <small class="text-muted">سيظهر هذا الاسم في URL مدونتك</small>
                                </div>

                                <div class="mb-3">
                                    <label for="password" class="form-label">كلمة المرور</label>
                                    <input type="password" class="form-control" id="password" name="password" 
                                           placeholder="اختر كلمة مرور قوية" required minlength="6">
                                </div>

                                <div class="mb-3">
                                    <label for="confirm_password" class="form-label">تأكيد كلمة المرور</label>
                                    <input type="password" class="form-control" id="confirm_password" name="confirm_password" 
                                           placeholder="أعد كتابة كلمة المرور" required minlength="6">
                                </div>

                                <div class="mb-3">
                                    <label for="bio" class="form-label">النبذة الشخصية (اختيارية)</label>
                                    <textarea class="form-control" id="bio" name="bio" rows="3" 
                                              placeholder="اكتب نبذة مختصرة عن نفسك وخبراتك..."></textarea>
                                </div>

                                <div class="d-grid">
                                    <button type="submit" class="btn btn-dark btn-lg">
                                        <i class="fas fa-user-plus me-2"></i>إنشاء الحساب والبدء
                                    </button>
                                </div>
                            <% } else { %>
                                <!-- إذا لم يكن الكود محققاً بعد -->
                                <div class="d-grid">
                                    <button type="submit" class="btn btn-dark btn-lg">
                                        <i class="fas fa-check me-2"></i>تحقق من الكود
                                    </button>
                                </div>
                            <% } %>
                        </form>

                        <div class="mt-4 text-center">
                            <div class="mb-2">
                                <small class="text-muted">لم تتلق كود التفعيل؟</small>
                            </div>
                            <a href="/blog-request" class="text-dark small">أنشئ طلب جديد</a>
                            <span class="text-muted mx-2">|</span>
                            <a href="/login" class="text-dark small">تسجيل الدخول</a>
                        </div>

                        <div class="mt-3">
                            <div class="alert alert-info">
                                <small>
                                    <i class="fas fa-info-circle me-2"></i>
                                    كود التفعيل صالح لمدة 7 أيام من تاريخ الإرسال
                                </small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer>
        مدونات آفاق | منصة للتدوين الثقافي والفكري © 2025
    </footer>

    <script>
        // التأكد من تطابق كلمات المرور
        document.getElementById('confirm_password')?.addEventListener('input', function() {
            const password = document.getElementById('password').value;
            const confirmPassword = this.value;
            
            if (password !== confirmPassword) {
                this.setCustomValidity('كلمات المرور غير متطابقة');
            } else {
                this.setCustomValidity('');
            }
        });

        // تنسيق حقل كود التفعيل
        document.getElementById('invite_code').addEventListener('input', function() {
            this.value = this.value.toUpperCase().replace(/[^A-Z0-9]/g, '');
        });
    </script>
</body>

</html>