<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>طلب إنشاء مدونة - مدونات آفاق</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@300;500;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.rtl.min.css" rel="stylesheet">    <link rel="stylesheet" href="/js/css/home.css">
    <link rel="stylesheet" href="/js/css/footer.css">
    <link rel="stylesheet" href="/js/css/ipad-menu.css">
    <style>        .form-container {
            max-width: 800px;
            margin: 0 auto;
        }
        
        .form-section {
            margin-bottom: 2rem;
            padding: 1.5rem;
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            background: #fafafa;
        }
        
        .form-section h3 {
            color: #8c4a21ca;
            margin-bottom: 1rem;
            font-size: 1.2rem;
            font-weight: 600;
        }
        
        .form-label {
            font-weight: 500;
            color: #333;
            margin-bottom: 0.5rem;
        }
        
        .form-control, .form-select {
            border: 1px solid #ddd;
            border-radius: 6px;
            padding: 0.75rem;
            font-size: 0.95rem;
        }
        
        .form-control:focus, .form-select:focus {
            border-color: #8c4a21ca;
            box-shadow: 0 0 0 0.2rem rgba(140, 74, 33, 0.15);
        }
        
        .alert {
            border-radius: 8px;
            margin-bottom: 1.5rem;
        }
        
        .btn-submit {
            background-color: #8c4a21ca;
            border: none;
            padding: 0.8rem 2rem;
            font-size: 1rem;
            font-weight: 500;
            border-radius: 6px;
            color: #fff;
            transition: all 0.3s ease;
        }
        
        .btn-submit:hover {
            background-color: #7a3e1d;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(140, 74, 33, 0.3);
        }
        
        .page-header {
            text-align: center;
            margin-bottom: 3rem;
            padding: 2rem 0;
        }
        
        .page-header h1 {
            color: #8c4a21ca;
            font-size: 2.5rem;
            font-weight: 600;
            margin-bottom: 1rem;
        }
        
        .page-header p {
            color: #666;
            font-size: 1.1rem;
            max-width: 600px;
            margin: 0 auto;
        }
        
        .required {
            color: #e74c3c;
        }
    </style>
</head>

<body>    <%- include('partials/navbar') %><main>
        <div class="container mb-5">
            <div class="page-header">
                <h1>طلب إنشاء مدونة</h1>
                <p>نرحب بانضمامك إلى مجتمع الكتّاب والمفكرين. يرجى تعبئة النموذج أدناه مع تدوينة نموذجية لمراجعة طلبك</p>
            </div>            <% if (error) { %>
                <div class="alert alert-danger" role="alert">
                    <%= error %>
                </div>
            <% } %>            <% if (success) { %>
                <div class="alert alert-success" role="alert">
                    <%= success %>                    <hr>
                    <div class="mt-2">
                        <a href="/blog-request-status" class="btn btn-sm btn-outline-success">
                            تتبع حالة الطلب
                        </a>
                    </div>
                </div>
            <% } %>

            <div class="form-container">
                <form method="POST" action="/blog-request">                    <!-- معلومات شخصية -->
                    <div class="form-section">
                        <h3>المعلومات الشخصية</h3>
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label for="fullName" class="form-label">الاسم الكامل <span class="required">*</span></label>
                                <input type="text" class="form-control" id="fullName" name="fullName" required>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="email" class="form-label">البريد الإلكتروني <span class="required">*</span></label>
                                <input type="email" class="form-control" id="email" name="email" required>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label for="specialty" class="form-label">التخصص أو المجال <span class="required">*</span></label>
                                <input type="text" class="form-control" id="specialty" name="specialty" required 
                                       placeholder="مثال: علوم سياسية، أدب، تاريخ، إلخ">
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="experienceYears" class="form-label">سنوات الخبرة <span class="required">*</span></label>
                                <input type="number" class="form-control" id="experienceYears" name="experienceYears" 
                                       min="0" max="50" required>
                            </div>
                        </div>
                    </div>                    <!-- التدوينة النموذجية -->
                    <div class="form-section">
                        <h3>التدوينة النموذجية</h3>
                        <div class="mb-3">
                            <label for="sampleTitle" class="form-label">عنوان التدوينة <span class="required">*</span></label>
                            <input type="text" class="form-control" id="sampleTitle" name="sampleTitle" required
                                   placeholder="اكتب عنواناً جذاباً ومعبراً">
                        </div>
                        <div class="mb-3">
                            <label for="sampleCategory" class="form-label">التصنيف <span class="required">*</span></label>
                            <select class="form-select" id="sampleCategory" name="sampleCategory" required>
                                <option value="">اختر التصنيف</option>
                                <option value="politics">السياسة</option>
                                <option value="history">التاريخ</option>
                                <option value="economics">الاقتصاد</option>
                                <option value="technology">التكنولوجيا</option>
                                <option value="literature">الأدب</option>
                                <option value="medicine">الطب</option>
                            </select>
                        </div>
                        <div class="mb-3">
                            <label for="sampleContent" class="form-label">محتوى التدوينة <span class="required">*</span></label>                            <textarea class="form-control" id="sampleContent" name="sampleContent" rows="10" required
                                      placeholder="اكتب تدوينة كاملة تعبر عن أسلوبك في الكتابة ومستوى المحتوى المتوقع... (الحد الأدنى 250 كلمة)"></textarea>
                            <div class="form-text">يرجى كتابة تدوينة كاملة تعكس مستوى وجودة المحتوى الذي تنوي نشره</div>
                        </div>
                    </div>                    <!-- الدافع للتدوين -->
                    <div class="form-section">
                        <h3>الدافع للتدوين</h3>
                        <div class="mb-3">
                            <label for="motivation" class="form-label">لماذا تريد إنشاء مدونة على منصتنا؟ <span class="required">*</span></label>
                            <textarea class="form-control" id="motivation" name="motivation" rows="4" required
                                      placeholder="اشرح دوافعك للتدوين وما تأمل في تحقيقه من خلال المنصة..."></textarea>
                        </div>                    </div>                    <div class="text-center mb-4">
                        <button type="submit" class="btn btn-submit">
                            إرسال الطلب
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </main>

    <footer>
        مدونات آفاق | منصة للتدوين الثقافي والفكري © <%= new Date().getFullYear() %>
    </footer>    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/ipad-menu.js"></script>
    <script>
        // عداد الكلمات للتدوينة النموذجية - تم تحديث الحد الأدنى إلى 250 كلمة
        const sampleContent = document.getElementById('sampleContent');
        const wordCountDiv = document.createElement('div');
        wordCountDiv.className = 'form-text mt-1';
        wordCountDiv.id = 'wordCount';
        sampleContent.parentNode.appendChild(wordCountDiv);        function updateWordCount() {
            const text = sampleContent.value.trim();
            const words = text ? text.split(/\s+/).length : 0;
            wordCountDiv.textContent = `عدد الكلمات: ${words} (الحد الأدنى: 250)`;            
            if (words < 250) {
                wordCountDiv.style.color = '#e74c3c';
                wordCountDiv.innerHTML = `عدد الكلمات: ${words} (الحد الأدنى: 250)`;
            } else {
                wordCountDiv.style.color = '#27ae60';
                wordCountDiv.innerHTML = `عدد الكلمات: ${words}`;
            }
        }

        sampleContent.addEventListener('input', updateWordCount);
        updateWordCount();

        // التحقق من صحة النموذج قبل الإرسال
        const form = document.querySelector('form');
        form.addEventListener('submit', function(e) {
            const text = sampleContent.value.trim();
            const words = text ? text.split(/\s+/).length : 0;
              if (words < 250) {
                e.preventDefault();
                alert('يجب أن تحتوي التدوينة النموذجية على 250 كلمة على الأقل');
                sampleContent.focus();
                return false;
            }

            // التأكد من ملء جميع الحقول المطلوبة
            const requiredFields = form.querySelectorAll('[required]');
            for (let field of requiredFields) {
                if (!field.value.trim()) {
                    e.preventDefault();
                    alert(`يرجى ملء الحقل: ${field.previousElementSibling.textContent}`);
                    field.focus();
                    return false;
                }
            }

            // التحقق من صحة البريد الإلكتروني
            const email = document.getElementById('email');
            const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!emailRegex.test(email.value)) {
                e.preventDefault();
                alert('يرجى إدخال عنوان بريد إلكتروني صحيح');
                email.focus();
                return false;
            }            // إضافة تأثير تحميل على الزر
            const submitBtn = form.querySelector('.btn-submit');
            submitBtn.innerHTML = 'جارٍ الإرسال...';
            submitBtn.disabled = true;
        });

        // تأثيرات الحركة
        window.addEventListener('scroll', () => {
            const navbar = document.querySelector('.navbar');
            if (window.scrollY > 50) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });
    </script>
</body>
</html>
